🤖 الگوی اتوماسیون UI.Vision RPA: اعتبارسنجی انبوه کارت‌ها (Data-Driven Card Validation)
این ماکرو یک نمونه آموزشی پیشرفته برای نشان دادن قدرت تکرار داده‌محور (Data-Driven RPA) در UI.Vision است. این اسکریپت وظیفه اجرای خودکار یک فرآیند استاندارد اعتبارسنجی کارت در یک مؤسسه مالی داخلی را با سرعت و دقت بالا انجام می‌دهد.

✨ ویژگی‌های اصلی اسکریپت
پردازش انبوه (Bulk Processing): خواندن و پردازش هزاران شماره کارت از یک فایل CSV.

استخراج نام دارنده کارت: اتوماسیون فرآیند استخراج نام دارنده کارت (خدمتی که در سامانه‌های همراه بانک، ATM و اینترنت بانک به صورت رایگان ارائه می‌شود).

سیستم حلقه پیشرفته: استفاده بهینه از دستور times و متغیر اختصاصی ${!times} برای اجرای دقیق لوپ.

مدیریت خروجی: ذخیره وضعیت نهایی، نام دارنده کارت و اطلاعات تأییدیه در یک فایل CSV مجزا.

مدیریت خطای فعال (Error Handling): زیرساخت لازم برای تشخیص خطاهای سیستمی (مانند منقضی بودن کارت) و ثبت آن در خروجی.

🛠️ پیش‌نیازها
برای اجرای موفقیت‌آمیز این ماکرو، باید موارد زیر فراهم باشد:

ابزار UI.Vision RPA: نصب شده روی مرورگر (Chrome/Firefox).

حساب کاربری فعال: برای استفاده از سرویس، باید یک حساب کاربری فعال و معتبر نزد مؤسسه مالی هدف داشته باشید.

فایل CSV ورودی: فایلی به نام file-5-data.csv (یا نام دلخواه با تنظیمات ماکرو) حاوی شماره کارت‌های مورد نظر در ستون اول.

⚙️ نکات مهم در سفارشی‌سازی و اجرا
این ماکرو برای یک سناریوی عمومی طراحی شده است. برای انطباق با محیط کاربری اختصاصی شما، حتماً باید تغییرات زیر را اعمال کنید:

۱. تنظیم انتخاب حساب مبدأ (ضروری)
از آنجا که دسترسی به این سرویس مستلزم داشتن یک حساب بانکی فعال است، ماکرو باید دقیقاً بداند که کدام حساب شما را به عنوان مبدأ برای اعتبارسنجی انتخاب کند.

خط مربوط به انتخاب حساب مبدأ در کد شما، آدرس XPath سطر سوم جدول حساب‌ها را هدف قرار داده است:

JSON

"Target": "xpath=//*[@id=\"ContentPlaceHolder1_txtFomToCard_grdFromCardList\"]/tbody/tr[3]/td[1]"
اگر حساب کاربری شما متفاوت باشد، ممکن است حساب مبدأ شما در سطر سوم (tr[3]) نباشد.

تغییر مورد نیاز: این XPath را ویرایش کنید تا آدرس دقیق حساب مبدأ دلخواه شما را هدف قرار دهد. (مثلاً اگر حساب شما سطر اول است، آن را به tr[1] تغییر دهید).

۲. تنظیم زمان‌های انتظار (Wait Times)
زمان انتظار waitForElementVisible در کد (۵ ثانیه) ممکن است برای اتصال اینترنت یا سرور شما کوتاه یا طولانی باشد. در صورت بروز خطا به دلیل کُندی سایت، این مقدار را افزایش دهید.

💡 اهداف آموزشی
این پروژه یک نمونه عملی برای یادگیری موارد زیر در اتوماسیون است:

CSV Reading Advanced: استفاده همزمان از csvReadArray برای شمارش و csvRead با متغیر !csvReadLineNumber برای خواندن دقیق سطرها.

Dynamic Looping: استفاده از متغیر سیستمی ${!csvReadMaxRow} برای تنظیم خودکار طول حلقه (times).

Text Extraction: استفاده از storeText برای ذخیره نتایج عملیات و تأییدیه‌ها.

Sequential Logic: طراحی یک جریان منطقی و گام به گام (ورود داده، ثبت، انتظار، استخراج نتیجه، ذخیره).
