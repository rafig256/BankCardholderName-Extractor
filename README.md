# 🤖 الگوی اتوماسیون UI.Vision RPA: اعتبارسنجی انبوه کارت‌ها (Data-Driven Card Validation)

**توضیحات کوتاه:** الگوی پیشرفته UI.Vision RPA برای اعتبارسنجی انبوه داده‌های کارت بانکی و استخراج نام دارنده از CSV. این ماکرو، کنترل حلقه پویا، ارسال فرم داده‌محور و تکنیک‌های مدیریت خطای قوی را نمایش می‌دهد. یک منبع آموزشی عالی برای یادگیری اتوماسیون پیچیده‌ی RPA و کار با داده‌های حساس مالی.

---

## ✨ ۱. ویژگی‌های اصلی اسکریپت

* **پردازش انبوه (Bulk Processing):** خواندن و پردازش هزاران شماره کارت از یک فایل **CSV**.
* **استخراج نام دارنده کارت:** اتوماسیون فرآیند استخراج نام دارنده کارت (خدمتی که در سامانه‌های بانکی به صورت رایگان ارائه می‌شود).
* **سیستم حلقه پیشرفته:** استفاده بهینه از دستور `times` و متغیر اختصاصی `${!times}` برای اجرای دقیق لوپ.
* **مدیریت خطای فعال (Error Handling):** زیرساخت لازم برای تشخیص خطاهای سیستمی و ثبت آن در خروجی با استفاده از `gotoIf_v2`.

---

## 🛠️ ۲. پیش‌نیازها

برای اجرای موفقیت‌آمیز این ماکرو، باید موارد زیر فراهم باشد:

1.  **ابزار UI.Vision RPA:** نصب شده روی مرورگر (Chrome/Firefox).
2.  **حساب کاربری فعال:** برای استفاده از سرویس، باید یک حساب کاربری فعال و معتبر نزد **مؤسسه مالی هدف** داشته باشید.
3.  **فایل CSV ورودی:** فایلی حاوی شماره کارت‌های مورد نظر در ستون اول.

---

## ⚙️ ۳. نکات مهم در سفارشی‌سازی و اجرا

این ماکرو برای یک سناریوی عمومی طراحی شده است. برای انطباق با محیط کاربری اختصاصی شما، **حتماً** باید تغییرات زیر را اعمال کنید:

### ۳.۱. تنظیم انتخاب حساب مبدأ (ضروری)

از آنجا که دسترسی به این سرویس مستلزم داشتن یک حساب بانکی فعال است، ماکرو باید دقیقاً بداند که کدام حساب شما را به عنوان **مبدأ** انتخاب کند.

* خط مربوط به انتخاب حساب مبدأ در کد شما، آدرس **XPath** سطر سوم جدول حساب‌ها را هدف قرار داده است (مثال):
    ```
    "Target": "xpath=//*[@id=\"ContentPlaceHolder1_txtFomToCard_grdFromCardList\"]/tbody/tr[3]/td[1]"
    ```
* **تغییر مورد نیاز:** این `XPath` را ویرایش کنید تا آدرس دقیق حساب **مبدأ دلخواه** شما را هدف قرار دهد. (مثلاً اگر حساب شما سطر اول است، آن را به `tr[1]` تغییر دهید).

### ۳.۲. تنظیم زمان‌های انتظار

* زمان انتظار `waitForElementVisible` ممکن است برای اتصال اینترنت یا سرور شما کوتاه یا طولانی باشد. در صورت بروز خطا به دلیل کُندی سایت، این مقدار را در بلوک کد مربوطه افزایش دهید.

---

## 💡 ۴. اهداف آموزشی و هشدارها (Disclaimer)

این پروژه یک نمونه عملی برای یادگیری موارد زیر در اتوماسیون است:

* **CSV Reading Advanced:** استفاده همزمان از `csvReadArray` برای شمارش و `csvRead` با متغیر `!csvReadLineNumber` برای خواندن دقیق سطرها.
* **Error Handling:** پیاده‌سازی مکانیزم `gotoIf_v2` و `!errorControl` برای پرش از خطاهای سیستمی و ثبت آن در خروجی.

---

### ⚠️ هشدار مهم (Terms of Service)

این ماکرو صرفاً به عنوان **نمونه آموزشی** برای یادگیری تکنیک‌های RPA ارائه شده است. کاربر مسئول است مطمئن شود که اجرای این ابزار در حجم بالا یا به صورت تجاری، قوانین و شرایط استفاده (Terms of Service) **مؤسسه مالی هدف** را نقض نمی‌کند.
